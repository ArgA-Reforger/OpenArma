[plugin]
summary = "Configuración de parámetros"
version = "0.0.2"
description = "Se usa normalmente para configurar parámetros del sistema de forma dinámica y mostrar datos en el frontend"
author = "wu-clan"
tags = ["other"]
database = ["mysql", "postgresql"]

[app]
extend = "admin"

[api.config]
prefix = "/configs"
tags = "系统参数配置"
